apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth-service
  ports:
  - port: 8010
    targetPort: 8000
  type: ClusterIP  # Or LoadBalancer/Ingress for external access